import 'package:flutter/material.dart';

class PriceInput extends StatelessWidget {
  const PriceInput({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(child: const TextField());
  }
}
