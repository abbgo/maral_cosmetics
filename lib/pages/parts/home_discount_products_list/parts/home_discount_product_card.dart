import 'package:flutter/material.dart';

class HomeDiscountProductCard extends StatelessWidget {
  const HomeDiscountProductCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Haryt');
  }
}
