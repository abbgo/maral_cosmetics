import 'package:flutter/material.dart';

class HomeNewBrendsSliderBody extends StatelessWidget {
  const HomeNewBrendsSliderBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Slider body');
  }
}
