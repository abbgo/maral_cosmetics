import 'package:flutter/material.dart';

class HomeNewBrendsSlider extends StatelessWidget {
  const HomeNewBrendsSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('In taze gosulanlar'),
    );
  }
}
